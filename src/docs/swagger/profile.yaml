paths:
  /api/profile/me:
    get:
      tags: [Profile]
      summary: Get logged-in user profile
      security:
        - bearerAuth: []
      responses:
        200:
          description: Profile fetched
        401:
          description: Unauthorized
